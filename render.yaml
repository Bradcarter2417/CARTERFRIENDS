services:
  - type: web
    name: carterfriends
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SESSION_SECRET
        value: carterfriends-secret-key
